      <% include partials/head %>
      <% include partials/header %>
      <% include partials/aside %>
      <main class="cms">
          <% if (logged_in !== logged_in) { %>
              <form class="userInput" action="/cms" method="post">
                  <label for="username">Accountnaam</label>
                  <input id="username" type="text" name="username">
                  <label for="password">Wachtwoord</label>
                  <input id="password" type="text" name="password">
                  <input type="submit" value="Submit">
              </form>
          <% } else{ %>
              <section>
              <h1>Data in Database</h1>

              <form>
                  <span class="search-icon"></span>
                  <input type="search" name="search" id="search" class="search" placeholder="voer een zoekopdracht in">
              </form>

            <a href="./cms/add" title="add a new sensor" class="button">Nieuwe sensor</a>

            <div>
              <table>
                  <tr>
                     <!-- <th>id</th> -->
                    <th>Name</th>
                    <th>Sector</th>
                    <th>Branch</th>
                    <th>Link</th>
                    <th>Contact</th>
                    <th>Aanpassen</th>
                    <th>Verwijderen</th>
                  </tr>

                  <% allSensors.forEach(function(sensor) { %>
                      <tr>
                          <!-- <td><%= sensor._id %></td> -->
                          <td><%= sensor.name %></td>
                          <td><%= sensor.sector %></td>
                          <td><%= sensor.branch %></td>
                              <% if(sensor.name !== "" && sensor.sector !== "" && sensor.branch !== ""){ %>
                                   <td><a href="/details/<%= sensor.sector[0]%>/<%= sensor.branch[0] %>/<%= sensor.name.replace(/ /g, '%20') %>">Link</a></td>
                              <% } else{ %>
                                  <td>Link</td>
                              <% } %>
                          <td><%= sensor.contactName %></td>
                          <td><a href="/cms/edit/<%= sensor._id %>">Aanpassen</a></td>
                          <td><a href="/cms/remove/<%= sensor._id %>">Verwijderen</a></td>
                      </tr>
                  <% }) %>
            </table>
          </div>
      </section>
      <% } %>
    </main>

  </body>
</html>
